[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "atomfun"
version = "0.1.0"
description = "Extract and catalog atomic functions from repositories."
dependencies = []

[project.scripts]
atomfun = "atomfun.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
